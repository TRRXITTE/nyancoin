cd /Users/traaitt/Documents/GitHub/app-bitcoin-new
docker run --rm -ti \
  -v "$(pwd):/app" \
  --user $(id -u):$(id -g) \
  ghcr.io/ledgerhq/ledger-app-builder/ledger-app-builder:latest \
  /bin/bash -lc 'export BOLOS_SDK=$NANOX_SDK; make clean; make COIN=nintondo TARGET_NAME=nanox DEBUG=1'




# Allow local connections to XQuartz
xhost +127.0.0.1

cd /Users/traaitt/Documents/GitHub/app-bitcoin-new

# Launch Speculos with Qt display
docker run --rm -it \
  -p 9999:9999 -p 5001:5000 \
  -e DISPLAY=host.docker.internal:0 \
  -v /tmp/.X11-unix:/tmp/.X11-unix \
  -v "$(pwd)/bin/app.elf:/speculos/app.elf" \
  ghcr.io/ledgerhq/ledger-app-builder/ledger-app-dev-tools:latest \
  speculos --model nanox /speculos/app.elf --display qt --api-port 5000 --apdu-port 9999 --hid




cd ~/nintondo-ledger
sudo docker run -it \
  --device /dev/bus/usb:/dev/bus/usb \
  -v "$(pwd):/app" \
  ghcr.io/ledgerhq/ledger-app-builder/ledger-app-builder:latest \
  /bin/bash -lc "cd /app && BOLOS_SDK=/opt/nanox-secure-sdk make clean && BOLOS_SDK=/opt/nanox-secure-sdk make load COIN=nintondo TARGET_NAME=TARGET_NANOX DEBUG=1"
